<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lh.app.member.mapper.MemberMapper">

	<sql id="criteria">
		<trim prefix="(" suffix=")AND" prefixOverrides="OR">
			<foreach collection="typeArr" item="type">
				<trim prefix="OR">
					<choose>
						<when test="type=='T'.toString()">
							ID like '%' ||#{keyword} || '%'
						</when>
						<when test="type=='C'.toString()">
							HOUSE_INFO like '%' ||#{keyword} || '%'
						</when>
						<when test="type=='A'.toString()">
							PHONE like '%' ||#{keyword} || '%'
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>

	<!-- 10/04 수정 시작 -->
	<select id="getTotalCount" resultType="int">
		SELECT COUNT(ROWNUM) FROM
		MEMBER
		WHERE
		<include refid="criteria"></include>
		ROWNUM > 0
	</select>
	<!-- 10/04 수정 끝 -->

	<select id="getList" resultType="memberVO">
		<![CDATA[
		SELECT *
		FROM (
			SELECT
			/*+INDEX_DESC(MEMBER MEMBER_PK)*/
			ROWNUM RN
			, ID
			, NAME
			, PHONE
			, HOUSE_INFO
			, AUTHOR
			, KEY
			FROM MEMBER
			WHERE
			]]>
		<include refid="criteria"></include>
			<![CDATA[
			ROWNUM <= #{pageNum} * #{amount}
		)
		WHERE RN > (#{pageNum} - 1) * #{amount}
	]]>
	</select>

	<select id="read" resultType="MemberInfoVO">
		SELECT DISTINCT M.ID, M.PHONE, M.HOUSE_INFO,
		M.PASSWORD,
		LISTAGG(C.CAR_CODE, '/') WITHIN GROUP (ORDER BY C.CAR_CODE) OVER(PARTITION BY
		C.HOUSE_INFO) AS carCode
		, LISTAGG(C.CAR_TYPE, '/') WITHIN GROUP (ORDER BY C.CAR_TYPE)
		OVER(PARTITION BY C.HOUSE_INFO) AS carType
		FROM MEMBER M
		JOIN GENERATION	G
		ON (M.HOUSE_INFO = G.HOUSE_INFO)
		JOIN CARLIST C
		ON (G.HOUSE_INFO = C.HOUSE_INFO)
		WHERE M.ID = #{id}
	</select>

	<!-- 10/04 추가 -->
	<select id="getListcar" resultType="MemberInfoVO">
		SELECT
		ROWNUM,C.CAR_TYPE,C.CAR_CODE
		FROM MEMBER M
		JOIN GENERATION G
		ON M.HOUSE_INFO = G.HOUSE_INFO
		JOIN CARLIST C
		ON G.HOUSE_INFO =
		C.HOUSE_INFO
		WHERE M.ID = #{id}
	</select>
	
	<!-- 10/04 19시 이후 커밋 -->
	<update id="updateCar">
		UPDATE CARLIST SET
		CAR_CODE = #{carCode},
		CAR_TYPE = #{carType}
		WHERE HOUSE_INFO = #{houseInfo}
		AND ROWNUM = #{rownum}
	</update>


	<delete id="delete">
		DELETE
		FROM MEMBER
		WHERE ID=#{id}
	</delete>


</mapper>